/*:
# Welcome to PlayDocs 🏓

### Cheat Sheet:
*To render markdown:* `Editor -> Show Rendered Markup`

*For raw markup:*  `Editor -> Show Raw Markup`

*Convert to markdown file:*  `playdocs convert Components --open`

*Convert to HTML file:*  `playdocs convert Components --html --open`
*/
import UIKit

var str = "Hello, PlayDocs!hi"

let components = str.components(separatedBy: "=>")
